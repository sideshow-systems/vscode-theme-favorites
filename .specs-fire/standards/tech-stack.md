# Tech Stack

## Overview

VS Code extension for managing and quickly switching between favorite themes. Built with TypeScript using the VS Code Extension API, compiled via tsc and published via vsce.

## Core Technologies

### Language

**Primary**: TypeScript
**Version**: ^4.9.5

### Framework

**Framework**: VS Code Extension API
**Version**: ^1.60.0
**Rationale**: Required for VS Code extension development; provides all hooks for commands, views, and settings.

### Runtime

**Runtime**: Node.js
**Version**: ^18.0.0 (dev), VS Code's bundled Node.js at runtime

## Data Layer

### Database

**Type**: None
**Database**: VS Code workspace/application settings (via `vscode.workspace.getConfiguration`)
**Version**: —

### ORM / Data Access

**Tool**: VS Code Configuration API
**Rationale**: Favorites are stored in VS Code's native settings store; no external DB needed.

### Caching

Not configured.

## API Layer

### API Style

Extension commands and TreeView providers (no HTTP API)

### Documentation

**Tool**: Inline JSDoc where needed; README.md for user-facing docs

## Frontend (if applicable)

### UI Framework

**Framework**: VS Code Webview API (custom HTML/CSS/JS panels)
**Version**: VS Code ^1.60.0

### Styling

**Approach**: Inline styles in webview HTML; VS Code theme variables for theming

### State Management

**Solution**: VS Code extension state via `globalState` / configuration

## Infrastructure

### Hosting

**Platform**: VS Code Marketplace (vsce publish)

### Containerization

Not configured.

### CI/CD

**Platform**: Not configured (manual `npm run build-package` + `vsce publish`)

## Development Tools

### Package Manager

**Manager**: npm

### Build Tool

**Tool**: tsc (TypeScript Compiler)

### Linting

**Linter**: None configured
**Config**: —

### Formatting

**Formatter**: Prettier
**Config**: package.json scripts (`npm run format`, `npm run format:check`)

## Dependencies

### Production Dependencies

*(none — VS Code API is a peer dependency provided by the host)*

### Development Dependencies

- `typescript` (^4.9.5) — TypeScript compiler
- `@types/node` (^18.0.0) — Node.js type definitions
- `prettier` (^3.8.3) — Code formatter
- `vscode` (^1.1.37) — VS Code type definitions and test runner shim

## Version Requirements

| Tool | Minimum Version | Recommended |
|------|-----------------|-------------|
| Node.js | 18.0.0 | 18.x LTS |
| npm | 8.0.0 | latest |
| TypeScript | 4.9.5 | 4.9.x |
| VS Code | 1.60.0 | latest |

---
*Generated by specs.md - fabriqa.ai FIRE Flow*
